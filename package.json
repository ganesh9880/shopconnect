{
  "name": "shopconnect",
  "private": true,
  "version": "1.0.0",
  "description": "Sri Lakshmi Vastralayam — digital shop platform (Phase 1 MVP)",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:prod": "npm run build --prefix frontend && echo Build done. Start with: cd backend && set NODE_ENV=production&& node src/index.js",
    "start:prod": "npm run start --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
