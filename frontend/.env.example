# Local dev: leave empty (Vite proxies /api to localhost:3001)
# Render static site: REQUIRED — your Web Service URL (where /api/health returns JSON, NOT the shop URL)
# Example: https://vastralayam-api.onrender.com
VITE_API_URL=

# Or edit public/config.json "apiUrl" and redeploy (if Render env does not apply)
